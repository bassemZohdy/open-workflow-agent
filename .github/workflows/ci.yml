name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  PYTHON_VERSION: "3.12"
  IMAGE_SIZE_LIMIT_BYTES: "2147483648"

jobs:
  root:
    name: Root quality gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Install Python
        run: uv python install "$PYTHON_VERSION"
      - name: Sync locked root environment
        run: uv sync --locked
      - name: Check root lock
        run: uv lock --check
      - name: Format
        run: uv run --locked ruff format --check core engines tests
      - name: Lint
        run: uv run --locked ruff check .
      - name: Type check
        run: uv run --locked mypy core/src
      - name: Tests
        run: uv run --locked pytest -q
      - name: Shared contract tests
        run: uv run --locked pytest tests/contract -q
      - name: Build root and standalone core artifacts
        run: |
          uv build
          uv build --directory core
      - name: Verify release metadata and lock inputs
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import tomllib
          from pathlib import Path

          expected = tomllib.loads(Path("pyproject.toml").read_text())['project']['version']
          for path in ("core/pyproject.toml", "engines/adk/pyproject.toml", "engines/langgraph/pyproject.toml"):
              actual = tomllib.loads(Path(path).read_text())['project']['version']
              assert actual == expected, f"{path} has version {actual}, expected {expected}"
          print(f"release version: {expected}")
          PY
          uv lock --check
          uv lock --directory engines/adk --check
          uv lock --directory engines/langgraph --check
      - name: Validate wheel contents
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import zipfile

          wheels = sorted(Path("dist").glob("*.whl")) + sorted(Path("core/dist").glob("*.whl"))
          assert wheels, "no wheels were built"
          for wheel in wheels:
              with zipfile.ZipFile(wheel) as archive:
                  names = archive.namelist()
                  assert any(name.endswith("open_workflow_agent/__init__.py") for name in names)
                  schema = "open_workflow_agent/_resources/open-workflow/1.0.3/workflow.yaml"
                  assert schema in names, f"missing packaged Open Workflow schema in {wheel.name}"
          PY

  engines:
    name: ${{ matrix.engine }} native and contract tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - engine: adk
            extra: native
            test_dir: adk
          - engine: langgraph
            extra: sqlite
            test_dir: langgraph
    defaults:
      run:
        working-directory: engines/${{ matrix.engine }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Install Python
        run: uv python install "$PYTHON_VERSION"
      - name: Check engine lock
        run: uv lock --check
      - name: Native and shared contract tests
        run: uv run --locked --extra ${{ matrix.extra }} --with pytest --with pytest-asyncio pytest ../../tests/${{ matrix.test_dir }} ../../tests/contract -q
      - name: Portable Profile CTK subset
        shell: bash
        run: |
          set -o pipefail
          uv run --locked --extra ${{ matrix.extra }} --with pytest --with pytest-asyncio \
            pytest ../../tests/ctk -q | tee "$RUNNER_TEMP/ctk-${{ matrix.engine }}.log"
      - name: Capture CTK provenance
        shell: bash
        run: |
          set -euo pipefail
          {
            echo "repository_commit=$(git rev-parse HEAD)"
            sed -n '1,8p' ../../tests/ctk/README.md
            echo "scenario_sha256="
            sha256sum ../../tests/ctk/features/*.feature
          } > "$RUNNER_TEMP/ctk-${{ matrix.engine }}-provenance.txt"
      - name: Upload CTK results and provenance
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ctk-${{ matrix.engine }}-results
          path: |
            ${{ runner.temp }}/ctk-${{ matrix.engine }}.log
            ${{ runner.temp }}/ctk-${{ matrix.engine }}-provenance.txt
          if-no-files-found: warn

  containers:
    name: Docker ${{ matrix.engine }} acceptance
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - engine: adk
            port: 18080
            resume_port: 19080
          - engine: langgraph
            port: 18081
            resume_port: 19081
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build independent runtime image
        run: docker build --build-arg OWA_VERSION=0.1.0 --build-arg VCS_REF=${{ github.sha }} --file docker/Dockerfile.${{ matrix.engine }} --tag owa-${{ matrix.engine }}:${{ github.sha }} .
      - name: Verify image metadata
        shell: bash
        run: |
          set -euo pipefail
          version="$(docker image inspect --format '{{ index .Config.Labels "org.opencontainers.image.version" }}' "owa-${{ matrix.engine }}:${{ github.sha }}")"
          revision="$(docker image inspect --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}' "owa-${{ matrix.engine }}:${{ github.sha }}")"
          test "$version" = "0.1.0"
          test "$revision" = "${{ github.sha }}"
      - name: Enforce image-size gate
        shell: bash
        run: |
          set -euo pipefail
          size="$(docker image inspect --format '{{.Size}}' owa-${{ matrix.engine }}:${{ github.sha }})"
          echo "owa-${{ matrix.engine }} size: ${size} bytes"
          if (( size > IMAGE_SIZE_LIMIT_BYTES )); then
            echo "image exceeds ${IMAGE_SIZE_LIMIT_BYTES}-byte limit" >&2
            exit 1
          fi
          {
            echo "### Docker image"
            echo "- Engine: ${{ matrix.engine }}"
            echo "- Size: ${size} bytes"
            echo "- Limit: ${IMAGE_SIZE_LIMIT_BYTES} bytes"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Container startup, health, capabilities, invocation, and knowledge
        shell: bash
        run: |
          set -euo pipefail
          workdir="$(mktemp -d)"
          mkdir -p "$workdir/config" "$workdir/knowledge" "$workdir/data"
          chmod 0777 "$workdir/data"
          cat > "$workdir/config/agent.yaml" <<'YAML'
          model:
            provider: fake
            name: fake/default
          knowledge:
            path: /knowledge
            database: /data/knowledge.sqlite3
            reload:
              mode: startup
          YAML
          printf '%s\n' 'CI knowledge acceptance document.' > "$workdir/knowledge/policy.md"
          name="owa-${{ matrix.engine }}-ci"
          log_file="$RUNNER_TEMP/owa-${{ matrix.engine }}-container.log"
          docker run --detach --name "$name" --publish ${{ matrix.port }}:8080 \
            --read-only --tmpfs /tmp:rw,nosuid,nodev,size=256m --user 12345:0 \
            --volume "$workdir/config:/config:ro" \
            --volume "$workdir/knowledge:/knowledge:ro" \
            --volume "$workdir/data:/data:rw" \
            "owa-${{ matrix.engine }}:${{ github.sha }}"
          cleanup() {
            docker logs "$name" > "$log_file" 2>&1 || true
            docker rm --force "$name" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT
          for attempt in $(seq 1 60); do
            if curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/health/ready" >/dev/null; then
              break
            fi
            sleep 1
          done
          curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/health/live" >/dev/null
          curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/health/ready" >/dev/null
          capabilities="$(curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/v1/capabilities")"
          python -c 'import json, sys; d=json.loads(sys.argv[1]); assert d["engine"] == sys.argv[2]; assert d["workflowDsl"] == "1.0.3"; assert d["features"]["resume"] is True' "$capabilities" "${{ matrix.engine }}"
          invocation="$(curl --fail --silent --request POST --header 'content-type: application/json' --data '{"input":{"question":"ci"}}' "http://127.0.0.1:${{ matrix.port }}/v1/invoke")"
          python -c 'import json, sys; d=json.loads(sys.argv[1]); assert d["status"] == "completed"; assert d["output"]["response"]["question"] == "ci"' "$invocation"
          reload="$(curl --fail --silent --request POST "http://127.0.0.1:${{ matrix.port }}/v1/admin/knowledge/reload")"
          python -c 'import json, sys; d=json.loads(sys.argv[1]); assert d["added"] == 0; assert d["unchanged"] == 1' "$reload"
      - name: Container stop, restart, and resume acceptance
        shell: bash
        run: |
          set -euo pipefail
          workdir="$(mktemp -d)"
          mkdir -p "$workdir/config" "$workdir/knowledge" "$workdir/data"
          chmod 0777 "$workdir/data"
          cat > "$workdir/config/agent.yaml" <<'YAML'
          model:
            provider: fake
            name: fake/default
          workflow:
            path: /config/resume.yaml
          knowledge:
            path: /knowledge
            database: /data/knowledge.sqlite3
            reload:
              mode: startup
          YAML
          cat > "$workdir/config/resume.yaml" <<'YAML'
          document:
            dsl: '1.0.3'
            namespace: ci
            name: restart-resume
            version: '1.0.0'
          do:
            - pause:
                wait:
                  seconds: 30
            - finish:
                set:
                  done: true
          YAML
          printf '%s\n' 'CI restart-resume acceptance document.' > "$workdir/knowledge/policy.md"
          name="owa-${{ matrix.engine }}-resume"
          resumed_name="${name}-restarted"
          log_file="$RUNNER_TEMP/owa-${{ matrix.engine }}-resume.log"
          resumed_log_file="$RUNNER_TEMP/owa-${{ matrix.engine }}-resume-restarted.log"
          cleanup() {
            docker logs "$name" > "$log_file" 2>&1 || true
            docker logs "$resumed_name" > "$resumed_log_file" 2>&1 || true
            docker rm --force "$name" "$resumed_name" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT
          docker run --detach --name "$name" --publish ${{ matrix.resume_port }}:8080 \
            --read-only --tmpfs /tmp:rw,nosuid,nodev,size=256m --user 12345:0 \
            --volume "$workdir/config:/config:ro" \
            --volume "$workdir/knowledge:/knowledge:ro" \
            --volume "$workdir/data:/data:rw" \
            "owa-${{ matrix.engine }}:${{ github.sha }}"
          for attempt in $(seq 1 60); do
            if curl --fail --silent "http://127.0.0.1:${{ matrix.resume_port }}/health/ready" >/dev/null; then
              break
            fi
            sleep 1
          done
          curl --fail --silent "http://127.0.0.1:${{ matrix.resume_port }}/health/ready" >/dev/null
          curl --fail --silent --request POST --header 'content-type: application/json' \
            --data '{"input":{}}' "http://127.0.0.1:${{ matrix.resume_port }}/v1/invoke" > "$workdir/invoke.json" &
          invoke_pid=$!
          invocation_id=""
          for attempt in $(seq 1 30); do
            invocation_id="$(python - "$workdir/data/runtime.sqlite3" <<'PY'
          import sqlite3
          import sys
          try:
              with sqlite3.connect(sys.argv[1]) as connection:
                  row = connection.execute(
                      "SELECT invocation_id FROM invocations WHERE status = 'running' ORDER BY rowid DESC LIMIT 1"
                  ).fetchone()
              print(row[0] if row else "")
          except sqlite3.Error:
              print("")
          PY
            )"
            if [[ -n "$invocation_id" ]]; then
              break
            fi
            sleep 1
          done
          test -n "$invocation_id"
          docker stop --time 1 "$name" >/dev/null
          kill "$invoke_pid" >/dev/null 2>&1 || true
          wait "$invoke_pid" >/dev/null 2>&1 || true
          docker rm "$name" >/dev/null
          docker run --detach --name "$resumed_name" --publish ${{ matrix.resume_port }}:8080 \
            --read-only --tmpfs /tmp:rw,nosuid,nodev,size=256m --user 12345:0 \
            --volume "$workdir/config:/config:ro" \
            --volume "$workdir/knowledge:/knowledge:ro" \
            --volume "$workdir/data:/data:rw" \
            "owa-${{ matrix.engine }}:${{ github.sha }}"
          for attempt in $(seq 1 60); do
            if curl --fail --silent "http://127.0.0.1:${{ matrix.resume_port }}/health/ready" >/dev/null; then
              break
            fi
            sleep 1
          done
          resume="$(curl --fail --silent --request POST --header 'content-type: application/json' \
            --data '{"input":{}}' "http://127.0.0.1:${{ matrix.resume_port }}/v1/invocations/${invocation_id}/resume")"
          python -c 'import json, sys; d=json.loads(sys.argv[1]); assert d["status"] == "completed"; assert d["output"] == {"done": True}' "$resume"
      - name: Upload container logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docker-${{ matrix.engine }}-logs
          path: ${{ runner.temp }}/owa-${{ matrix.engine }}-container.log
          if-no-files-found: ignore

  postgres:
    name: PostgreSQL persistence ${{ matrix.engine }} acceptance
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - engine: adk
            port: 18180
          - engine: langgraph
            port: 18181
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: owa
          POSTGRES_PASSWORD: owa
          POSTGRES_DB: owa
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U owa -d owa"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build PostgreSQL-enabled runtime image
        run: docker build --build-arg OWA_VERSION=0.1.0 --build-arg VCS_REF=${{ github.sha }} --file docker/Dockerfile.${{ matrix.engine }} --tag owa-${{ matrix.engine }}:postgres-${{ github.sha }} .
      - name: Start, restart, and invoke against PostgreSQL
        shell: bash
        run: |
          set -euo pipefail
          workdir="$(mktemp -d)"
          mkdir -p "$workdir/config" "$workdir/knowledge" "$workdir/data"
          chmod 0777 "$workdir/data"
          cat > "$workdir/config/agent.yaml" <<'YAML'
          model:
            provider: fake
            name: fake/default
          persistence:
            datasource: postgresql://owa:owa@host.docker.internal:5432/owa
          knowledge:
            path: /knowledge
            reload:
              mode: startup
          YAML
          printf '%s\n' 'PostgreSQL persistence acceptance document.' > "$workdir/knowledge/policy.md"
          name="owa-${{ matrix.engine }}-postgres"
          restarted_name="${name}-restarted"
          log_file="$RUNNER_TEMP/owa-${{ matrix.engine }}-postgres.log"
          restarted_log_file="$RUNNER_TEMP/owa-${{ matrix.engine }}-postgres-restarted.log"
          cleanup() {
            docker logs "$name" > "$log_file" 2>&1 || true
            docker logs "$restarted_name" > "$restarted_log_file" 2>&1 || true
            docker rm --force "$name" "$restarted_name" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT
          run_args=(
            --read-only
            --tmpfs /tmp:rw,nosuid,nodev,size=256m
            --user 12345:0
            --add-host host.docker.internal:host-gateway
            --volume "$workdir/config:/config:ro"
            --volume "$workdir/knowledge:/knowledge:ro"
            --volume "$workdir/data:/data:rw"
          )
          docker run --detach --name "$name" --publish ${{ matrix.port }}:8080 "${run_args[@]}" \
            "owa-${{ matrix.engine }}:postgres-${{ github.sha }}"
          for attempt in $(seq 1 60); do
            if curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/health/ready" >/dev/null; then
              break
            fi
            sleep 1
          done
          curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/health/ready" >/dev/null
          invocation="$(curl --fail --silent --request POST --header 'content-type: application/json' \
            --data '{"input":{"question":"postgres"}}' \
            "http://127.0.0.1:${{ matrix.port }}/v1/invoke")"
          python -c 'import json, sys; d=json.loads(sys.argv[1]); assert d["status"] == "completed"; assert d["output"]["response"]["question"] == "postgres"' "$invocation"
          docker stop --time 5 "$name" >/dev/null
          docker rm "$name" >/dev/null
          docker run --detach --name "$restarted_name" --publish ${{ matrix.port }}:8080 "${run_args[@]}" \
            "owa-${{ matrix.engine }}:postgres-${{ github.sha }}"
          for attempt in $(seq 1 60); do
            if curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/health/ready" >/dev/null; then
              break
            fi
            sleep 1
          done
          curl --fail --silent "http://127.0.0.1:${{ matrix.port }}/health/ready" >/dev/null
          curl --fail --silent --request POST --header 'content-type: application/json' \
            --data '{"input":{"question":"postgres-after-restart"}}' \
            "http://127.0.0.1:${{ matrix.port }}/v1/invoke" \
            | python -c 'import json, sys; d=json.load(sys.stdin); assert d["status"] == "completed"'
      - name: Upload PostgreSQL container logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docker-${{ matrix.engine }}-postgres-logs
          path: ${{ runner.temp }}/owa-${{ matrix.engine }}-postgres*.log
          if-no-files-found: ignore

  postgres-core:
    name: PostgreSQL common-store integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: owa
          POSTGRES_PASSWORD: owa
          POSTGRES_DB: owa
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U owa -d owa"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Install Python
        run: uv python install "$PYTHON_VERSION"
      - name: Run isolated PostgreSQL store tests
        env:
          OWA_TEST_POSTGRES_URL: postgresql://owa:owa@127.0.0.1:5432/owa
        run: uv run --locked --extra postgres pytest tests/core/test_postgres_persistence.py -q
