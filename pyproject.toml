[project]
name = "open-workflow-agent"
version = "0.1.0"
description = "Configuration-driven Open Workflow runtime with interchangeable engines"
license = "MIT"
license-files = ["LICENSE"]
readme = "README.md"
authors = [{ name = "Bassem Zohdy" }]
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.141.1",
  "httpx==0.28.1",
  "jsonschema==4.26.0",
  "pydantic==2.13.5",
  "PyYAML==6.0.3",
  "uvicorn==0.52.4",
]

[dependency-groups]
dev = [
  "pytest==9.1.1",
  "pytest-asyncio==1.4.0",
  "ruff==0.16.6",
  "mypy==2.3.1",
  "types-jsonschema==4.26.0.20260518",
  "types-PyYAML==6.0.12.20260815",
  "pytest-cov>=7.1.0",
]
mutation = [
  "mutmut==3.7.0",
]

[project.optional-dependencies]
model = ["litellm==1.100.0"]
knowledge = [
  "fastembed==0.8.0",
  "numpy==2.5.3",
  "pypdf==6.18.0",
]
postgres = ["psycopg[binary]==3.3.5"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["core/src/open_workflow_agent"]

[tool.pytest.ini_options]
pythonpath = [
  ".",
  "core/src",
  "engines/adk/src",
  "engines/langgraph/src",
  "engines/agent-framework/src",
]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
exclude = "^tests/"
mypy_path = [
  "core/src",
  "engines/adk/src",
  "engines/langgraph/src",
  "engines/agent-framework/src",
]

[[tool.mypy.overrides]]
module = [
  "open_workflow_agent_adk.*",
  "open_workflow_agent_langgraph.*",
  "open_workflow_agent_agent_framework.*",
]
# The native SDKs are optional and their type surfaces are incomplete. Keep
# strict checking for adapter-owned code while treating those imports as an
# explicitly bounded integration boundary.
ignore_missing_imports = true
warn_unused_ignores = false
disable_error_code = ["untyped-decorator"]

[[tool.mypy.overrides]]
module = [
  "agent_framework.*",
  "google.*",
  "langchain_core.*",
  "langgraph.*",
]
ignore_missing_imports = true

[tool.mutmut]
source_paths = ["src"]
only_mutate = [
  "src/open_workflow_agent/traffic_policy.py",
]
pytest_add_cli_args_test_selection = [
  "tests/mutation/test_traffic_policy_mutation.py",
]
pytest_add_cli_args = ["--override-ini=pythonpath=src"]
