document:
  dsl: '1.0.3'
  namespace: tests
  name: switch
  version: '1.0.0'
do:
  - route:
      switch:
        - priority:
            when: ${ .kind == 'priority' }
            then: continue
        - default:
            then: continue
