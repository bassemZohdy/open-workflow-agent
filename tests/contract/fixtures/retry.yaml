document:
  dsl: '1.0.3'
  namespace: tests
  name: retry
  version: '1.0.0'
do:
  - response:
      try:
        - model:
            call: llm:1.0.0@default
            with:
              prompt: retryable
      catch:
        retry:
          limit:
            attempt:
              count: 1
