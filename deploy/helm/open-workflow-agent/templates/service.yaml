apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-workflow-agent.fullname" . }}
  labels:
    {{- include "open-workflow-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "open-workflow-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
