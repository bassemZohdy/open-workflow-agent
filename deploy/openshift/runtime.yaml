# Reference OpenShift deployment for the Open Workflow Agent runtime images.
# OpenShift assigns an arbitrary non-root UID from the restricted-v2 SCC; the
# runtime images are verified to start under an arbitrary UID with a read-only
# root filesystem. Replace image tags with pinned digests and provide real
# configuration/knowledge sources and provider secrets before use.
#
# Scale-out note: replicas is intentionally 1 with a Recreate strategy; see
# docs/deployment.md for the single-runtime ownership constraints.
---
apiVersion: v1
kind: Namespace
metadata:
  name: owa
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owa-data
  namespace: owa
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: owa-langgraph
  namespace: owa
  labels:
    app.kubernetes.io/name: open-workflow-agent
    app.kubernetes.io/instance: owa-langgraph
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: open-workflow-agent
      app.kubernetes.io/instance: owa-langgraph
  template:
    metadata:
      labels:
        app.kubernetes.io/name: open-workflow-agent
        app.kubernetes.io/instance: owa-langgraph
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: runtime
          # Replace with an exact version or digest for production use.
          image: ghcr.io/bassemzohdy/open-workflow-agent-langgraph:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            - name: OWA__MODEL__PROVIDER
              value: fake
            - name: OWA__MODEL__NAME
              value: fake/default
            - name: OWA__SERVER__HOST
              value: 0.0.0.0
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          startupProbe:
            httpGet:
              path: /health/ready
              port: http
            periodSeconds: 5
            failureThreshold: 24
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
            - name: config
              mountPath: /config
            - name: knowledge
              mountPath: /knowledge
              readOnly: true
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: owa-data
        - name: tmp
          emptyDir:
            sizeLimit: 256Mi
        # Replace with a ConfigMap (runtime configuration/workflow) so the
        # runtime can load /config/agent.yaml.
        - name: config
          emptyDir: {}
        # Replace with a read-only volume holding your knowledge documents.
        - name: knowledge
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: owa-langgraph
  namespace: owa
  labels:
    app.kubernetes.io/name: open-workflow-agent
    app.kubernetes.io/instance: owa-langgraph
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: open-workflow-agent
    app.kubernetes.io/instance: owa-langgraph
  ports:
    - name: http
      port: 8080
      targetPort: http
